{
	"name": "blueprint-test",
	"version": "1.0.0",
	"description": "Blueprint setup",
	"homepage": "https://blueprint.test",
	"repository": {
	},
	"scripts": {
	  "prepare": "lerna bootstrap --hoist && husky install && composer install",
	  "build": "lerna run build --stream",
	  "start": "lerna run start --parallel",
	  "watch": "lerna run watch --parallel",
	  "test": "lerna run test --stream",
	  "clean": "lerna clean",
	  "clean-dist": "lerna run clean-dist --stream"
	},
	"engines": {
	  "node": ">=16.0.0",
	  "npm": ">=8.0.0"
	},
	"devDependencies": {
	  "husky": "^6.0.0",
	  "lerna": "^4.0.0",
	  "lint-staged": "^10.5.4",
	  "npm-run-all": "^4.1.5"
	},
	"dependencies": {
	  "postinstall": "^0.7.4"
	}
  }
  